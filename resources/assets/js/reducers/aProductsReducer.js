import * as types from "../actions/types";

const initialState = {
    products:[]
};

export const aProductsReducer = function (state = initialState , action) {
    switch (action.type) {
        default:
            return state
    }

};
